Example is meant for testing sprintf(), sprintl() and sprinti() functions using mikroSDK 2.0.

The test result can be viewed via UART or application output, depending on the selected setup.
User-defined LEDs will turn on for each of the three tests if successful.

Go step by step through the example and follow instructions for testing.
